#pragma once

#include "Fish.h"
/*
class NormalFish : Fish
{
	NormalFish(Point position, float direction = 0)
	{
		Fish(position, direction, FishSettings());
	}
	NormalFish() : Fish() {}
};

class BigFish : Fish
{
	BigFish(Point position, float direction = 0)
	{
		FishSettings settings = FishSettings();
		settings.color_R = 0;
		settings.color_G = 0.6;
		settings.color_B = 0;

		settings.speed = 0.5;
		settings.size = 3;

		settings.grouping = false;
		settings.independence = true;

		Fish(position, direction, settings);
	}
	BigFish() : Fish() {}
};

class SmallFish : Fish
{
	SmallFish(Point position, float direction = 0)
	{
		FishSettings settings = FishSettings();
		settings.color_R = 1;
		settings.color_G = 1;
		settings.color_B = 0;

		settings.size = 0.5;

		Fish(position, direction, settings);
	}
	SmallFish() : Fish() {}
};

class FastFish : Fish
{
	FastFish(Point position, float direction = 0)
	{
		FishSettings settings = FishSettings();
		settings.color_R = 1;
		settings.color_G = 0;
		settings.color_B = 0;

		settings.speed = 20;

		Fish(position, direction, settings);
	}
	FastFish() : Fish() {}
};
*/